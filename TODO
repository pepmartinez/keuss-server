* REST
* STOMP
  + add tests, tests, tests
  + add auth?
  + add http introspection (minimally done)
  + implement keepalives
  + add finer grain config for:
    - delay/exp-decay on nack
  + profile cpu/mem
  + add subscribe support for:
    - window size
    - parallel consumers

