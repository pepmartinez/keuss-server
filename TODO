* REST
* STOMP
  + add tests, tests, tests
  + add http instrocpection
  + implement keepalives
  + add filer grain config for:
    - delay on nack
  + profile
  + add subscribe support for:
    - window size
    - parallel consumers

