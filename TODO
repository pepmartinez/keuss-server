# TODO

* General
  + ability to disable stomp & amqp on config
* REST
* STOMP
  + add auth
  + add TLS
  + profile cpu/mem
* AMQP
  + add auth
* MQTT
  + add it?

* Exchanges
  + Reviewing the documentacion of keuss, I cannot find where we documented the `mature`, `delay` and `tries` properties for the push() operation. Ill like to put a link to that from the exchange doc

 * documentation

 * blog
   + article on buckets
   + article on ETLs/Pipelines
   + article on streams, with scalability tests
   + compared performance tests with other MQs in same-host conditions
     - write a test tool